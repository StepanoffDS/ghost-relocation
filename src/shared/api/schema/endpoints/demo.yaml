reset:
  post:
    operationId: resetDemo
    requestBody:
      required: true
      content:
        application/json:
          schema: { $ref: ../shared/schemas.yaml#/components/schemas/DemoResetRequest }
    responses:
      '200':
        description: Active fixture name.
        content:
          application/json:
            schema: { $ref: ../shared/schemas.yaml#/components/schemas/DemoResetResponse }
